SpectralCloudstering
**********************
This is the main function of ``SCIMES`` and the one to be called to
run the dendrogram clustering.
The class called several function to generate the affinity matrices,
rescale them, estabilish the number of clusters, run the actual
spectral clustering, and cleaning the sparse clusters.

.. autofunction:: scimes.scimes.SpectralCloudstering
